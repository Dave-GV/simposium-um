spring.application.name=simposium
spring.config.import=optional:file:.env[.properties]

# Base de datos (dev)
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.kkrrsvbcrcvitvvxregb
spring.datasource.password=Simposi-UMFITEC

# JPA - Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.default_schema=dashboard

# JWT
app.jwt.secret=${APP_JWT_SECRET:MiClaveSecretaMuyLargaYSegura1234567890ABCDEF}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

# Usuario demo (solo para desarrollo)
app.demo-user.email=${APP_DEMO_USER_EMAIL:demo@simposium.com}
app.demo-user.password=${APP_DEMO_USER_PASSWORD:demo123}

